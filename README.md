"# Company Project" 
📘 First Demo Project

A simple project created to practice Git commands, branching workflows, remote repository setup, and handling multiple Git accounts (personal + company).

🚀 Project Overview

This project is used for:

Learning and practicing Git (beginner → advanced)

Working with branches and merge workflows

Managing GitHub remote repositories

Testing company vs personal Git account configuration

Understanding commit history and project versioning

📁 Project Structure
/CompanyProject
 ├── README.md
 ├── index.html        (optional)
 ├── src/              (future source code)
 └── assets/           (images, css, js files)

🧠 Git Commands Practiced
Initialization
git init
git status

Staging & Committing
git add .
git commit -m "Initial commit"

Branching
git checkout -b feature-branch
git merge feature-branch

Remote Repository Setup
git remote add origin https://github.com/USERNAME/firstdemo.git
git remote -v
git push -u origin main

Pulling Updates
git pull


git add .
git commit -m "Added test file"
git push

